{
  "name": "shell",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "craco start --port 3000 --open",
    "build": "craco build",
    "test": "craco test"
  },
  "dependencies": {
    "@craco/craco": "^7.1.0",
    "react-scripts": "5.0.1"
  }
}
